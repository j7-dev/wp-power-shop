export const appName = window?.appData?.APP_NAME || 'Power Shop'
export const kebab = window?.appData?.KEBAB
export const snake = window?.appData?.SNAKE
export const baseUrl = window?.appData?.BASE_URL || '/'
export const renderId1 = window?.appData?.RENDER_ID_1 || 'my-app'
export const renderId2 = window?.appData?.RENDER_ID_2 || 'my-app'
export const renderId3 = window?.appData?.RENDER_ID_3 || 'my-app'
export const renderId4 = window?.appData?.RENDER_ID_4 || 'my-app'

export const apiUrl = window?.wpApiSettings?.root || '/wp-json'
export const ajaxUrl = window?.appData?.ajaxUrl || '/wp-admin/admin-ajax.php'
export const siteUrl = window?.appData?.siteUrl || '/'
export const checkoutUrl = window?.appData?.checkoutUrl || '/checkout'
export const ajaxNonce = window?.appData?.ajaxNonce || ''
export const currentUserId = window?.appData?.userId || '0'
export const postId = window?.appData?.postId || '0'
export const permalink = window?.appData?.permalink || '/'

export const apiTimeout = window?.appData?.API_TIMEOUT || '30000'

// export const defaultImage = `${siteUrl}/wp-content/power-shop-release/js/dist/defaultImage.jpg`

export const elApiKey = 'F9B52619-A5AC9B31-F7E931B1-526AD47D'

export const colorPrimary = window?.appData?.colorPrimary || '#1677ff'
export const isConfetti = window?.appData?.isConfetti || true
